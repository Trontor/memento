/**
 * TODO: Family related tests
 */
describe("Easy Test Suite", () => {
  describe("Verify Math still exists and works as expected", () => {
    it("1 + 1 should equal 2", () => {
      expect(1 + 1).toBe(2);
    });
  });
});
