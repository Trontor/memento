overwrite: true
schema: "schema.graphql.ts"
documents: null
generates:
  generated/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
  graphql.schema.json:
    plugins:
      - "introspection"
